# Optional: use a standalone PostgreSQL server instead of embedded PostgreSQL.
# DATABASE_URL=postgresql://otpusk:otpusk@localhost:5432/otpusk
PORT=3001
HOST=127.0.0.1
COMPANY_TIMEZONE=Asia/Qyzylorda
# Demo identity switching is for local drafts only. Server refuses production mode.
DEMO_MODE=true
